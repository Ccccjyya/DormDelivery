# 2026-07-24 部署进度日志

## 已完成
- 环境 ID 填入 cloudbase.js（cloud1-d0g51zhvo764bc013）
- npm install 完成（268 包，node_modules 存在）
- 用户已在 CloudBase 控制台「云函数」上传 initDatabase 并云端测试：成功建了 11 个集合，但**索引因云端无腾讯云密钥被安全跳过**（代码兜底行为，符合设计）
- 用户已**删除云端 initDatabase 云函数**（本地文件夹留着，无害）

## 当前卡点（未解决）
- **前端构建失败**：`miniapp/node_modules/@dcloudio/uni-cli-shared` 包损坏，缺 package.json（其余 17 个 @dcloudio 包正常）。导致 `uni build -p mp-weixin` 报 "Cannot find module '@dcloudio/uni-cli-shared'"。
- `.bin` 目录为空，`uni` bin 符号链接未生成；构建需用 `node node_modules/@dcloudio/vite-plugin-uni/bin/uni.js build -p mp-weixin` 绕开。
- 索引 31 个尚未建（文档表格 31 行，非正文写的 28）；需手动控制台建，或配 SECRET_ID/KEY 后重跑 initDatabase。

## 待用户决定
- 是否授权只重装 `@dcloudio/uni-cli-shared` 这一个包来修复前端构建（不动其他、不删文件）
- 索引采用手动建还是配密钥重跑

## 下午进展（12:00-13:40）
- 用户自己跑了 `npm install`，顺手修好了 uni-cli-shared 坏包（package.json 回来了，但 .bin/uni 符号链接仍没有 → `npm run build:mp-weixin` 依旧报 "uni 不是内部或外部命令"；构建要用 `node node_modules/@dcloudio/vite-plugin-uni/bin/uni.js build -p mp-weixin`）
- **前端构建已成功**：dist/build/mp-weixin 产物已生成并验证

## UI 改版（浅蓝校园风，结构不变）
- 用户要求：我的页面风格不协调、去 AI 味、校园青春风、**浅蓝色**、结构不变、修复顶部与手机状态栏不匹配
- 项目里已有一版 tabbar 新页面（pages/tabbar-home/orders/messages/profile + components/TabBar.vue + StatusBar.vue + TaskTypeSheet.vue，原珊瑚橙 #FF7043 系；pages.json 里 tabBar custom:true，navigationStyle:custom）
- 截图里的"绿色+Emoji 列表"版本与当前代码不符（应是更早迭代的产物），以当前 tabbar-profile 代码为准改的
- **浅蓝色板**（全项目统一）：主色 #3E9BF0、深色文字/强调 #2E8FD9、渐变 #3E9BF0→#63B5F6→#9FD4FA、页面底 #F3F8FD、浅徽章底 #E3F1FD/#EAF4FD/#E8F2FB/#D6EBFD/#BBDEFB/#C2E2FC/#D8ECFE、次要文字 #7A93A8/#8AA3B8、主文字 #1B3A57/#2A4257、边框 #D8E9F7/#C9DFF2、箭头 #BCCFDE、阴影 rgba(62,155,240,x) / rgba(46,143,217,x)；配送中状态用语义琥珀色 #B7791F/#FDF0D5；退出红 #E57373 保留
- 改动范围：tabbar-profile（整段样式重写）、TabBar.vue、tabbar-home、tabbar-orders、tabbar-messages、App.vue 全局色板、pages.json selectedColor，以及其余 20 个内层页面（含 pages-admin/pages-super/shared/TaskTypeSheet）的旧青绿色批量映射，共约 90+ 处
- 注意：tabbar-profile 里有一段 AI 生成的"死代码"图标样式（mask 方案 + ::after 箭头，后面被 SVG data-uri 方案和 display:none 覆盖），未清理仅换色
- 顶部沉浸：tabbar 页面已是 fixed top:0 + StatusBar 组件撑状态栏高度，结构保持
- 构建验证通过（DONE Build complete），产物 wxss 已含新蓝色值
- **待办**：用户在微信开发者工具里重新编译查看效果；`npm run build:mp-weixin` 的 uni bin 缺失问题仍在（要么用 node 直调 uni.js，要么重装依赖恢复 .bin）；31 个索引仍未建

## 顶部遮挡修复（14:03）
- 用户反馈首页（tabbar-home）顶部"同学你好呀"文字被状态栏区域遮挡
- 根因：StatusBar.vue 用了旧版回调 API `uni.getSystemInfo({success(){}})`，首次渲染时可能拿不到正确的 statusBarHeight（初始值仅 20px，iPhone X+ 设备实际约 44px）
- 修复：改用同步 API `uni.getSystemInfoSync()`，确保首次渲染即拿到正确高度；同时微调 page-header padding 给文字更多呼吸空间
- 构建已通过验证

## 发布任务拆为 4 个独立页面（14:35）
- 用户要求：task-publish 从"共用页面+tab 切换"改回"4 个独立页面"
- 新建 4 页：task-publish-takeout（外卖）/ package（快递）/ grocery（帮买）/ printing（打印）
- 每页只有自己的专属字段（外卖=取餐码、快递=单号、帮买=清单、打印=文件名），不混用 v-if
- pages.json 移除 task-publish/index，加 4 个新路由（均用原生导航栏，带标题+返回按钮）
- TabBar.vue handleSelect 改为 pageMap 跳对应独立页面
## 外卖代取 + 上传图片（14:39）
- 在 task-publish-takeout 页面新增图片上传区域（完成时限和备注之间）
- 最多 4 张，支持相册和拍照，自动压缩
- 缩略图网格预览，点击放大、右上角叉删除
- 构建通过

## 外卖代取 + 取件方式切换（14:42）
- 在表单顶部加二选一开关：宿舍楼下 vs 外卖驿站
- 宿舍楼下：自动填入用户 profile 里的 dormSnapshot.fullRoomLabel（如"B03号楼 5层 12室"），只读蓝底展示
- 外卖驿站：显示输入框自由填写驿站位置
- 通过 userStore.profile 和 uni.getStorageSync('cloudProfile') 双重兜底获取宿舍信息
- 构建通过

## 贡献值体系重构（14:48-14:54）
- **移除**：发单次数(postingQuota)、十级贡献值档位体系、每周扣除/配额结算
- **新规则**：发单时消耗贡献值（1-30 可自定义，默认 5），订单完成后贡献值转给接单人
- **后端**：businessRules.js 简化；orders.js create 扣贡献值替代扣 quota；complete 记录 rewardAmount 由 maintenance 结算转账；auth.js/users.js 移除 postingQuota
- **前端**：4 个发布页均加贡献值投入选择器（3/5/8/10/15 五档，显示余额）；我的页面移除「剩余发单」和「发单次数记录」
- **构建通过**
